import { FormLogin as Login } from './Login'
import { FormPost as Post } from './Post'
import { FormProfile as Profile } from './Profile' 
import { FormComment as Comment } from './Comment'

export const Form = {
    Login,
    Post,
    Profile,
    Comment,
}